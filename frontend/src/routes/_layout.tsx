import React from "react";

const Layout = () => {
  return (
    <div id='layout' className="flex h-full w-full flex-col items-center justify-center">
      <h1>Layout</h1>
    </div>
  ); 
};

export default Layout;
