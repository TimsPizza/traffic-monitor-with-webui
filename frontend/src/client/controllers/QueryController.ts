import {
  IByDestinationPort,
  IByProtocol,
  IBySourceIP,
  IBySourceRegion,
  IByTimeRange,
  IByTopSourceIps,
  IProtocolAnalysis,
  IProtocolDistribution,
  ITimeSeries,
  ITrafficSummary,
} from "../api/models/request";
import { QueryService } from "../services/query";
import { TQueryType } from "../types";

export class QueryController {
  public static queryBy = () => {
    
  }
}
